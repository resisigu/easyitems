# EasyItems Resource Pack

This directory is reserved for the generated EasyItems resource pack.

The next implementation step will generate the pack automatically from the item definitions in config.yml.
